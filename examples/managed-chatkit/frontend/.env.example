# Copy to `.env` and fill in values.

# Optional (if omitted, backend must have CHATKIT_WORKFLOW_ID set).
VITE_CHATKIT_WORKFLOW_ID=

# Optional (dev server proxies /api to this target).
VITE_BACKEND_TARGET=http://localhost:8009

# Optional (set this if you want to bypass the proxy and call backend directly).
# Tip: if you see CORS errors, prefer leaving this empty and relying on `VITE_BACKEND_TARGET` proxy.
VITE_BACKEND_ORIGIN=
