# @xpert-ai/chatkit-web-shared

This package contains shared web utilities and primitives used across ChatKit web packages.

## Role in the ChatKit project

- Provides shared helpers, constants, and browser-facing utilities reused by the iframe UI and web component.
- Keeps web-specific logic centralized to avoid duplication across packages.
- Acts as the common dependency layer for ChatKit web integrations.
