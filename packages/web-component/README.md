# Xpert Chatkit Web Component

This package ships the ChatKit Web Component for embedding ChatKit in any web page.

## Role in the ChatKit project

- Exposes a framework-agnostic custom element for integrating ChatKit.
- Manages the iframe lifecycle and wiring between the host page and the ChatKit UI.
- Serves as the primary distribution artifact for non-React web integrations.
